name: Archived - Legacy wallet rebuild
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo 'This legacy workflow is retired. Use the U.S. opportunity scan and desk validation workflows.'
